const rewire = require("rewire")































































































































































































































































const index = rewire("../index")































































































































































































































































const calcTotal = index.__get__("calcTotal")































































































































































































































































// @ponicode
describe("calcTotal", () => {
    test("0", () => {
        calcTotal([1, 2, 3, 4, 5])
    })

    test("1", () => {
        calcTotal(5)
    })

    test("2", () => {
        calcTotal("this is test")
    })
})
